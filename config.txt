# Magic: The Gathering Lackey CCG Plugin Config
# Output file where card lists will be appended (relative to script location, in ./sets/ folder)
output_file=custom.txt

# Set codes to fetch and process (comma-separated, e.g., tla,lci,eld)
set_codes=tla,lci

# Download images for cards (true/false)
download_images=true
